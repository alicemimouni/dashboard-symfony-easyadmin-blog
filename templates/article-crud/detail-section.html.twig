{% extends 'base-admin.html.twig' %}

{% block title %}Détail de la section{% endblock %}

{% block body %}

<div class="d-flex align-items-center justify-content-center">
    <div class="container m-3">
        <h1 class="m-5 text-center">Section</h1>
        <table class="table">
            <tbody>
                <tr>
                    <th>Titre de la section</th>
                    <td>{{ section.name }}</td>
                </tr>
                <tr>
                    <th>Contenu</th>
                    <td>{{ section.textContent }}</td>
                </tr>
            </tbody>
        </table>
        <div class="text-center">
            <a href="{{ path('app_article_index') }}">Retour</a>
        </div>
        {{ include('article-crud/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
